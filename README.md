# Readme

## Sql Table Creation Query.

```sql
CREATE TABLE Country (
    country_id SERIAL PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    first_letter CHAR(1) NOT NULL,
    last_letter CHAR(1) NOT NULL
);

CREATE TABLE State (
    state_id SERIAL PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    country_id INT NOT NULL,
    first_letter CHAR(1) NOT NULL,
    last_letter CHAR(1) NOT NULL,
    is_capital_state BOOLEAN NOT NULL DEFAULT FALSE,
    FOREIGN KEY (country_id) REFERENCES Country(country_id)
);

CREATE TABLE City (
    city_id SERIAL PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    state_id INT NOT NULL,
    country_id INT NOT NULL,
    is_prime_length BOOLEAN NOT NULL,
    is_capital_city BOOLEAN NOT NULL DEFAULT FALSE,
    FOREIGN KEY (state_id) REFERENCES State(state_id),
    FOREIGN KEY (country_id) REFERENCES Country(country_id)
);

-- Indexes
CREATE INDEX idx_country_name ON Country (name);
CREATE INDEX idx_country_first_letter ON Country (first_letter);
CREATE INDEX idx_country_last_letter ON Country (last_letter);
CREATE INDEX idx_state_name ON State (name);
CREATE INDEX idx_state_first_letter ON State (first_letter);
CREATE INDEX idx_state_last_letter ON State (last_letter);
CREATE INDEX idx_city_name ON City (name);
CREATE INDEX idx_city_prime_length ON City (is_prime_length);
CREATE INDEX idx_state_is_capital_state ON State (is_capital_state);
CREATE INDEX idx_city_is_capital_city ON City (is_capital_city);
```

## Project Structure

```
project_root/
│
├── src/                           # Source code directory
│   ├── __init__.py                # Init file to make src a package
│   │
|   ├── data/                          # Directory for storing data files
|   │   ├── countries.csv              # CSV file containing country data
|   │   ├── states.csv                 # CSV file containing state data
|   │   └── cities.csv                 # CSV file containing city data
|   | 
│   ├── database/                  # Module for database related operations
│   │   ├── __init__.py
│   │   ├── db_connector.py        # Module for database connection
│   │   ├── db_models.py           # Module defining database models
│   │   └── db_operations.py       # Module for database CRUD operations
│   │
│   ├── data_processing/           # Module for data processing
│   │   ├── __init__.py
│   │   ├── data_loader.py         # Module for loading data from CSV files
│   │   ├── data_extractor.py      # Module for extracting data from the internet
│   │   └── data_transformer.py    # Module for transforming data
│   │
│   ├── operations/                # Module for user input operations
│   │   ├── __init__.py
│   │   ├── countries_operations.py # Module for country operations
│   │   ├── states_operations.py   # Module for state operations
│   │   └── cities_operations.py   # Module for city operations
|   |
│   ├── decorators/
│   │   ├── __init__.py
│   │   └── track_time_and_memory.py   # Decorator to track time and memory
│   │
│   └── main.py                    # Main entry point of the application
│
├── tests/                         # Directory for unit tests
│   ├── __init__.py
│   ├── test_database.py           # Unit tests for database module
│   ├── test_data_processing.py    # Unit tests for data processing module
│   ├── test_operations.py         # Unit tests for operations module
│   └── test_main.py               # Unit tests for main functionality
│
├── run.py                         # Code execution driver
├── README.md                      # Project README file
├── Pipfile                        # Pipenv file for managing dependencies
├── Pipfile.lock                   # Lock file generated by pipenv
└── .gitignore                     # Git ignore file
```
